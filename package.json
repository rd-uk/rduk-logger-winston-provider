{
  "name": "@rduk/logger-winston-provider",
  "version": "0.1.0",
  "description": "Winston implementation of abstract logger base provider",
  "main": "lib/index.js",
  "dependencies": {
    "@rduk/logger": "0.1.0",
    "winston": "2.4.0"
  },
  "devDependencies": {
    "coveralls": "3.0.0",
    "istanbul": "0.4.5",
    "jasmine": "2.8.0",
    "jshint": "2.9.5"
  },
  "scripts": {
    "lint": "jshint lib/*.js lib/**/*.js",
    "pretest": "npm run lint && cp ./spec/resources/config*.yml .",
    "test": "istanbul cover node_modules/jasmine/bin/jasmine.js --report cobertura",
    "posttest": "rm -rf ./config*yml && rm out.log && istanbul report && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "rduk",
    "logger",
    "winston"
  ],
  "author": "RDUK <tech@rduk.fr>",
  "license": "MIT"
}
